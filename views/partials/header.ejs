<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>KopiKoni | <%= typeof title !== 'undefined' ? title : 'Selamat Datang' %></title>
  <link href="/css/output.css" rel="stylesheet">
  <link href="/vendor/aos/aos.css" rel="stylesheet">

  <!-- <style>
  html, body {
    height: 100%;
    margin: 0;
    padding: 0;
    overflow: hidden; /* Pastikan ini tidak memblokir dropdown yang berada di luar main-content-area */
  }

  .flex-1.flex.flex-col.min-w-0 { 
      height: 100%;
  }

  #main-content-area {
    flex-grow: 1;
    overflow-y: auto; /* Ini yang menyebabkan scrolling di area utama */
    -webkit-overflow-scrolling: touch;
  }

  /* Ini tidak akan mempengaruhi interaksi dropdown, hanya tampilan scrollbar */
  .horizontal-scrollbar-hidden {
    -ms-overflow-style: none;
    scrollbar-width: none;
  }
  .horizontal-scrollbar-hidden::-webkit-scrollbar {
    display: none;
  }
</style> -->
</head>
<body class="bg-gray-900 text-gray-100 font-sans antialiased flex min-h-screen">

  <%- include('sidebar', { 
    user: typeof user !== 'undefined' ? user : null,
    latestOrderStatus: typeof latestOrderStatus !== 'undefined' ? latestOrderStatus : null,
    // --- PENTING: activeMenu DITERUSKAN KE SIDEBAR ---
    activeMenu: typeof activeMenu !== 'undefined' ? activeMenu : '' 
  }) %>

  <div class="flex-1 flex flex-col min-w-0">
        <% // Komponen Notifikasi Flash Message %>
        <% if (typeof message !== 'undefined' && message && message.text) { %>
            <div id="flash-notification" class="fixed top-5 right-5 z-50 p-6 rounded-xl shadow-lg max-w-sm
                <%= message.type === 'success' ? 'bg-green-600 text-white' : 'bg-red-600 text-white' %>" 
                data-aos="fade-left">
              <div class="flex items-center">
                <span class="text-3xl mr-4"><%= message.type === 'success' ? '🎉' : '❌' %></span>
                <div>
                  <h3 class="text-xl font-bold"><%= message.type === 'success' ? 'Berhasil!' : 'Terjadi Kesalahan!' %></h3>
                  <p class_="<%= message.type === 'success' ? 'text-green-100' : 'text-red-100' %>"><%= message.text %></p>
                </div>
              </div>
            </div>
        <% } %>

        <main id="main-content-area" class="flex-1 bg-gray-900 overflow-y-auto">