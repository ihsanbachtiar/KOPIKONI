<%- include('../partials/header', { title: 'Keranjang Saya' }) %> 
<main class="flex-1 p-8 bg-gray-900 text-white overflow-y-auto" id="main-content-area">
<section class="container mx-auto px-4 py-8 relative"> 
  <!-- <% // Menggunakan 'message' yang dikirim dari backend, bukan 'success_message' %>
  <% if (message && message.text) { %>
    <div id="success-notification" class="fixed top-24 right-8 z-50 p-6 rounded-xl shadow-lg max-w-sm
        <%= message.type === 'success' ? 'bg-green-600 text-white' : 'bg-red-600 text-white' %>" 
        data-aos="fade-left">
      <div class="flex items-center">
        <span class="text-3xl mr-4">🎉</span>
        <div>
          <h3 class="text-xl font-bold"><%= message.type === 'success' ? 'Pesanan Berhasil!' : 'Terjadi Kesalahan!' %></h3>
          <p class="<%= message.type === 'success' ? 'text-green-100' : 'text-red-100' %>"><%= message.text %></p>
        </div>
      </div>
    </div>
  <% } %> -->


  <h1 class="text-4xl font-extrabold text-amber-300 mb-10">Keranjang Saya</h1>

  <div class="bg-gray-800 rounded-xl shadow-lg border border-gray-700 overflow-hidden">
    
    <% if (items && items.length > 0) { %>

      <table class="w-full text-left text-gray-300">
        <thead class="bg-gray-700 uppercase text-sm text-gray-400">
          <tr>
            <th class="py-3 px-6">Produk</th>
            <th class="py-3 px-6">Harga Satuan</th>
            <th class="py-3 px-6">Kuantitas</th>
            <th class="py-3 px-6">Subtotal</th>
            <th class="py-3 px-6">Aksi</th>
          </tr>
        </thead>
        <tbody>
          <% items.forEach(itemData => { %>
            <tr class="border-b border-gray-700 hover:bg-gray-700 transition-colors">
              
              <td class="py-4 px-6">
                <div class="flex items-center space-x-4">
                  <img src="<%= itemData.item.image ? itemData.item.image : '/images/placeholder-menu.jpg' %>" alt="<%= itemData.item.name %>" class="w-16 h-16 object-cover rounded-md">
                  <div>
                    <div class="font-medium text-white text-lg"><%= itemData.item.name %></div>
                    <div class="text-sm text-gray-400"><%= itemData.item.category_name || '' %></div>
                  </div>
                </div>
              </td>
              
              <td class="py-4 px-6 text-lg">
                <%= itemData.item.price.toLocaleString('id-ID', { style: 'currency', currency: 'IDR', maximumFractionDigits: 0 }) %>
              </td>
              
              <td class="py-4 px-6 text-lg text-center"><%= itemData.qty %></td>
              
              <td class="py-4 px-6 text-lg font-semibold text-amber-400">
                <%= itemData.price.toLocaleString('id-ID', { style: 'currency', currency: 'IDR', maximumFractionDigits: 0 }) %>
              </td>
              
              <td class="py-4 px-6">
                <form action="/order/remove/<%= itemData.item.menu_id %>" method="POST">
                  <button type="submit" class="bg-red-700 hover:bg-red-800 text-white text-sm font-bold py-2 px-3 rounded-md transition-all">
                    Hapus
                  </button>
                </form>
              </td>
            </tr>
          <% }); %>
        </tbody>
      </table>

      <div class="p-6 bg-gray-700 flex justify-between items-center">
        <div>
          <span class="text-2xl font-bold text-white">Total Keranjang:</span>
          <span class="text-3xl font-extrabold text-amber-300 ml-4">
            <%= totalPrice.toLocaleString('id-ID', { style: 'currency', currency: 'IDR', maximumFractionDigits: 0 }) %>
          </span>
        </div>
        <div>
          <button id="checkout-popup-btn" class="bg-green-600 hover:bg-green-700 text-white font-semibold py-3 px-8 rounded-lg shadow-lg transform hover:scale-105 transition-all duration-300">
            Lanjut ke Checkout
          </button>
        </div>
      </div>

    <% } else { %>
      <div class="p-12 text-center">
        <h2 class="text-2xl font-semibold text-gray-400 mb-4">Keranjang Anda kosong.</h2>
        <p class="text-gray-500 mb-8">Sepertinya Anda belum menambahkan menu apapun.</p>
        <a href="/dashboard" class="bg-amber-600 hover:bg-amber-700 text-white font-semibold py-3 px-8 rounded-lg shadow-lg transform hover:scale-105 transition-all duration-300">
          Mulai Belanja
        </a>
      </div>
    <% } %>

  </div>
</section>

<div id="checkout-modal" class="fixed inset-0 bg-black bg-opacity-75 flex items-center justify-center z-50 hidden" data-aos="fade-in">
  <div class="bg-gray-800 p-8 rounded-2xl shadow-xl w-full max-w-lg border border-gray-700 relative" data-aos="fade-up" data-aos-delay="100">
    
    <button id="close-modal-btn" class="absolute top-4 right-5 text-gray-500 hover:text-white text-3xl">&times;</button>
    
    <h2 class="text-3xl font-bold text-amber-300 mb-6">Formulir Checkout</h2>
    
   <form action="/order/checkout" method="POST" enctype="multipart/form-data">
            <div class="mb-4">
              <label for="customer_name" class="block text-gray-300 text-sm font-bold mb-2">Nama Lengkap:</label>
              <input type="text" id="customer_name" name="customer_name" 
                     value="<%= user.name || '' %>"
                     class="shadow appearance-none border border-gray-700 rounded w-full py-2 px-3 bg-gray-700 text-white leading-tight focus:outline-none focus:shadow-outline focus:border-amber-500" required>
            </div>
            <div class="mb-6">
              <label for="customer_address" class="block text-gray-300 text-sm font-bold mb-2">Alamat Pengiriman:</label>
              <textarea id="customer_address" name="customer_address" rows="3"
                        class="shadow appearance-none border border-gray-700 rounded w-full py-2 px-3 bg-gray-700 text-white leading-tight focus:outline-none focus:shadow-outline focus:border-amber-500" required><%= user.address || '' %></textarea>
            </div>

            <div class="mb-6">
              <label for="payment_method_id" class="block text-gray-300 text-sm font-bold mb-2">Metode Pembayaran:</label>
              <select id="payment_method_id" name="payment_method_id"
                      class="shadow border border-gray-700 rounded w-full py-2 px-3 bg-gray-700 text-white leading-tight focus:outline-none focus:shadow-outline focus:border-amber-500" required>
                <option value="">Pilih Metode Pembayaran</option>
                <% if (paymentMethods && paymentMethods.length > 0) { %>
                  <% paymentMethods.forEach(method => { %>
                    <option value="<%= method.method_id %>"><%= method.method_name %></option>
                  <% }); %>
                <% } %>
              </select>
            </div>

            <div class="mb-6">
              <label for="payment_proof" class="block text-gray-300 text-sm font-bold mb-2">Bukti Pembayaran (Gambar):</label>
              <input type="file" id="payment_proof" name="payment_proof" accept="image/*"
                     class="block w-full text-sm text-gray-400
                            file:mr-4 file:py-2 file:px-4
                            file:rounded-full file:border-0
                            file:text-sm file:font-semibold
                            file:bg-amber-600 file:text-white
                            hover:file:bg-amber-700
                            cursor-pointer bg-gray-700 rounded-lg border border-gray-600 focus:outline-none">
              <p class="text-xs text-gray-500 mt-1">*Wajib jika menggunakan Transfer Bank / E-Wallet</p>
            </div>

            <button type="submit" class="w-full bg-green-600 hover:bg-green-700 text-white font-bold py-3 px-4 rounded-lg transition-colors focus:outline-none focus:shadow-outline">
              Konfirmasi & Bayar
            </button>
          </form>

    </form>
  </div>
</div>

</main> <% // Menutup tag main yang dibuka di awal %>
<%- include('../partials/footer') %>